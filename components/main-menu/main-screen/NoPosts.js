import React from "react";

const NoPosts = () => {
  return (
    <div className="l-1-r nopost-container">
      Your collection is empty at the moment, add something to start with!
    </div>
  );
};

export default NoPosts;
